extends ../../layout/default.pug 
include ../../mixins/box-head.pug

block main 
    .container.my-3 
        .row 
            .col-md-12 
                +box-head('Topics Page')
        .row 
            each topic in listTopic
                .col-md-6.mb-3 
                    div( class= "card") 
                        img(src=topic.avatar, alt="")
                        div( class= "card-body") 
                            h5( class= "card-title") #{topic.title}
                            p( class= "card-text") #{topic.description}
                            a( href=`/songs/${topic.slugTopic}`, class= "btn btn-primary") Xem chi tiết
            
