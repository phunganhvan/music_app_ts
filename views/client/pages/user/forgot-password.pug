extends ../../layout/default.pug
include ../../mixins/alert.pug

block main
    

    .container.my-5
        .row.justify-content-center
            .col-8.col-md-6.col-lg-4
                .card.shadow.p-4
                    h1.text-center.mb-4 Lấy lại mật khẩu
                    form(
                        action="/user/password/forgot"
                        method="POST"
                    )
                        .mb-3
                            label.form-label(for="email") Email
                            input#email.form-control(
                                type="email"
                                name="email"
                                required
                                placeholder="Nhập địa chỉ email của bạn"
                            )
                        button.btn.btn-primary.w-100.mt-3(type="submit")
                            i.bi.bi-envelope.me-2
                            | Gửi mail xác nhận

block styles 
    style.
        //- Container và căn giữa
        .container.my-5 {
            padding-top: 60px !important;
            padding-bottom: 60px !important;
            /* Rất quan trọng: Đảm bảo container có chiều cao đủ để căn giữa theo chiều dọc */
            min-height: calc(100vh - 80px); /* Điều chỉnh (100vh - chiều cao header/footer) */
            background: #E8EDF1;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        //- /* KHÔNG CẦN thay đổi CSS cho .row và .col, chúng ta đã đặt flex/center trên container */

        //- /* Card chứa Form */
        .card.shadow.p-4 {
            background: #FFFFFF;
            border-radius: 12px;
            padding: 40px !important;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1), 0 0 5px rgba(0, 0, 0, 0.05);
            border: none;
            transition: transform 0.3s ease;
            width: 100%; /* Giữ nguyên width 100% để nó lấp đầy column Bootstrap */
            
            /* Đã thêm: Đảm bảo nội dung không bị overflow hoặc căn chỉnh sai trong Card */
            box-sizing: border-box; 
        }

        .card.shadow.p-4:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.15), 0 0 10px rgba(0, 0, 0, 0.08);
        }

        //- /* HEADING */
        .card h1.text-center.mb-4 {
            //- /* Áp dụng lại phong cách Metal Gradient cho tiêu đề */
            color: #3A4958;
            font-weight: 700;
            font-size: 2rem;
            background: linear-gradient(90deg, #88AABB, #4B5B6D); 
            background-clip: text;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin-bottom: 30px !important; 
            white-space: nowrap; /* Tránh tiêu đề bị xuống dòng không cần thiết */
        }

        //- /* FORM LABELS */
        .card .form-label {
            font-weight: 600;
            color: #4B5B6D;
            margin-bottom: 5px;
            display: block;
        }

        //- /* FORM INPUTS */
        .card .form-control {
            border-radius: 8px;
            border: 1px solid #D1D9E6; 
            padding: 10px 15px;
            font-size: 1rem;
            box-shadow: inset 0 1px 3px rgba(0, 0, 0, 0.05);
            transition: all 0.3s ease;
        }

        .card .form-control:focus {
            border-color: #4B5B6D; 
            box-shadow: 0 0 0 0.25rem rgba(75, 91, 109, 0.25); 
        }

        /* SUBMIT BUTTON (btn-primary) */
        .card button.btn.btn-primary {
            background: linear-gradient(90deg, #4B5B6D 0%, #293846 100%);
            color: #FFFFFF;
            font-weight: 600;
            border: none;
            border-radius: 8px;
            padding: 12px 20px;
            font-size: 1.1rem;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 4px 10px rgba(41, 56, 70, 0.3); 
            display: flex;
            justify-content: center;
            align-items: center;
            white-space: nowrap; /* Quan trọng: Ngăn chữ nút bị xuống dòng */
        }

        .card button.btn.btn-primary:hover {
            background: linear-gradient(90deg, #5A6D80 0%, #354554 100%); 
            transform: translateY(-2px); 
            box-shadow: 0 6px 15px rgba(41, 56, 70, 0.4);
            color: #FFFFFF;
        }

        .card button.btn.btn-primary i {
            font-size: 1.25rem;
        }

        //- /* ==================================== */
        //- /* 📱 RESPONSIVE ADJUSTMENTS */
        //- /* ==================================== */
        @media (max-width: 576px) {
            .container.my-5 {
                padding: 30px 15px !important;
            }
            .card.shadow.p-4 {
                padding: 30px 25px !important;
            }
            .card h1.text-center.mb-4 {
                font-size: 1.5rem; /* Giảm kích thước heading trên mobile */
            }
        }
       
