extends ../../layout/default.pug 


block main 

    .container.my-5 
        .row.justify-content-center 
            .col-8 
                h1(class="text-center") Nhập mã OTP xác thực
                form(
                    action="/user/password/otp"
                    method="POST"
                )
                    div(class="mb-3")
                        label(for="email") Email 
                        input(
                            type="email"
                            class="form-control"
                            id="email"
                            name="email"
                            required 
                            value= email 
                            readonly 
                        )
                    div(class="mb-3")
                        label(for="otp") Mã OTP 
                        input(
                            type="text"
                            class="form-control"
                            id="otp"
                            name="otp"
                            required 
                        )
                    div(class= "mb-3")
                        button(
                            type="submit"
                            class="btn btn-primary w-100"
                        ) Xác nhận

block styles 
    style.
        /* ==================================== */
        /* 🔑 OTP VERIFICATION PAGE STYLES */
        /* ==================================== */

        /* Bố cục trang chung */
        .container.my-5 {
            padding-top: 60px !important;
            padding-bottom: 60px !important;
            min-height: calc(100vh - 80px); /* Đảm bảo căn giữa theo chiều dọc */
            background: #E8EDF1; 
            display: flex;
            justify-content: center;
            align-items: center;
        }

        /* Áp dụng một thẻ bao bọc (Card-like structure) cho form */
        /* Do cấu trúc Pug không có .card, chúng ta sẽ áp dụng style cho .col-8 để mô phỏng thẻ card */
        .row.justify-content-center > .col-8 {
            background: #FFFFFF;
            border-radius: 12px;
            padding: 40px;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1), 0 0 5px rgba(0, 0, 0, 0.05);
            transition: transform 0.3s ease;
            max-width: 450px; /* Giới hạn chiều rộng của form */
            width: 100%;
        }

        .row.justify-content-center > .col-8:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.15), 0 0 10px rgba(0, 0, 0, 0.08);
        }

        /* HEADING */
        .row.justify-content-center > .col-8 h1 {
            //- Áp dụng lại phong cách Metal Gradient cho tiêu đề
            color: #3A4958;
            font-weight: 700;
            font-size: 2rem;
            background: linear-gradient(90deg, #88AABB, #4B5B6D); 
            background-clip: text;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin-bottom: 30px !important; 
            white-space: nowrap; 
        }

        /* FORM LABELS */
        .col-8 label {
            font-weight: 600;
            color: #4B5B6D; 
            margin-bottom: 5px;
            display: block;
        }

        /* FORM INPUTS */       
        .col-8 .form-control {
            border-radius: 8px;
            border: 1px solid #D1D9E6; 
            padding: 10px 15px;
            font-size: 1rem;
            box-shadow: inset 0 1px 3px rgba(0, 0, 0, 0.05);
            transition: all 0.3s ease;
        }

        .col-8 .form-control:focus {
            border-color: #4B5B6D; 
            box-shadow: 0 0 0 0.25rem rgba(75, 91, 109, 0.25); 
        }

        /* Input Email (Readonly) */
        #email.form-control {
            background-color: #F8F9FA; /* Nền hơi xám cho input chỉ đọc */
            opacity: 0.8;
            cursor: default;
        }


        /* SUBMIT BUTTON (btn-primary) */
        .col-8 button.btn.btn-primary {
            /* Sử dụng gradient style */
            background: linear-gradient(90deg, #4B5B6D 0%, #293846 100%);
            color: #FFFFFF;
            font-weight: 600;
            border: none;
            border-radius: 8px;
            padding: 12px 20px;
            font-size: 1.1rem;
            cursor: pointer;
            transition: all 0.3s ease;
            box-shadow: 0 4px 10px rgba(41, 56, 70, 0.3); 
        }

        .col-8 button.btn.btn-primary:hover {
            background: linear-gradient(90deg, #5A6D80 0%, #354554 100%); 
            transform: translateY(-2px); 
            box-shadow: 0 6px 15px rgba(41, 56, 70, 0.4);
            color: #FFFFFF;
        }

        /* ==================================== */
        /* 📱 RESPONSIVE ADJUSTMENTS */
        /* ==================================== */
        @media (max-width: 576px) {
            .container.my-5 {
                padding: 30px 15px !important;
            }
            .row.justify-content-center > .col-8 {
                padding: 30px 25px;
            }
            .row.justify-content-center > .col-8 h1 {
                font-size: 1.5rem; 
            }
        }